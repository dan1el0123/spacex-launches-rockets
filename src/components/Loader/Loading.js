import "./Loading.css";

const Loading = () => {
    return (
        <div className="spinner">
            <div className="spinner__loading">🚀</div>
        </div>
    );
};

export default Loading;
